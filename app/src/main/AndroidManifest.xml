<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.localz">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".OrderDetailsSellerActivity"></activity>
        <activity android:name=".MainSellerActivity" />
        <activity android:name=".OrderUserDetailsActivity" />
        <activity android:name=".ProductActivity" />
        <activity android:name=".UserShopActivity" />
        <activity android:name=".UploadImage" />
        <activity android:name=".CartActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".RatingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>